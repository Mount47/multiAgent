---                                                                                         
  Harness 到底指什么
                                                                                              
  Harness = 包在模型外面的所有工程层。从"模型收到 prompt                                      
  前"到"模型输出被消费后"之间的一切基础设施：                                                 
                  
  用户输入
     ↓
  ┌─────────────────────────────────────────┐
  │ Harness 层                                │                                               
  │   上下文管理  路由/编排  工具调用          │                                              
  │   检索/记忆   重试/降级  观测/Trace         │                                             
  │   缓存        结构化输出  验证/安全         │                                             
  └─────────────────────────────────────────┘                                                 
     ↓                                                                                        
     模型（LLM）                                                                              
     ↑                                                                                        
  ┌─────────────────────────────────────────┐
  │ Harness 层（输出侧）                      │                                               
  │   解析  验证  路由下一步  写日志            │                                             
  └─────────────────────────────────────────┘                                                 
     ↓                                                                                        
  结果                                                                                        
                  
  Harness 不是"单 agent 的专属技术"——它是相对于"模型本体"的概念。单 agent 和多 agent 都有     
  harness 层，只是形态不同。
                                                                                              
  ---             
  单 agent 的 harness vs 多 agent 的 harness
                                            
  ┌────────────┬──────────────────────┬─────────────────────────────────────────┐
  │    能力    │ 单 agent 的 harness  │           多 agent 的 harness           │             
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 路由       │ 工具选择、skill 匹配 │ + agent 选择、FSM 流转                  │             
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 上下文     │ 历史压缩、RAG        │ + agent 间消息传递策略                  │             
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 观测       │ 一条长对话的 Trace   │ + 状态机时间线、agent 粒度 token 分桶   │             
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 重试       │ 工具失败重试         │ + 状态级重试、修订循环                  │
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 记忆       │ 统一一份             │ + 按 agent 分流（conv vs code）         │
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 人工介入   │ 审批单步             │ + 阶段级 checkpoint                     │
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 结构化输出 │ 拿一份 JSON          │ + guard 函数消费验证的 verdict          │
  ├────────────┼──────────────────────┼─────────────────────────────────────────┤             
  │ 缓存       │ prompt cache         │ + 状态输出复用、共享 system prompt 前缀 │
  └────────────┴──────────────────────┴─────────────────────────────────────────┘             
                  
  多 agent 的 harness 不是更少，而是更多——所有单 agent 的 harness 技术都在，另外多了一堆"agent
   之间"的 harness 问题。
                                                                                              
  ---             
  本项目本身就是 harness 工程的典型样本
                                                                                              
  回头看项目架构，几乎每一行非 AutoGen 的代码都是 harness：
                                                                                              
  ┌──────────────────────────────────────────┬────────────────────────────────────────────┐ 
  │                   模块                   │                Harness 分类                 │   
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤ 
  │ orchestration/state_machine.py + yaml    │ 路由 harness（替代 LLM selector            │ 
  │                                          │ 的确定性路由）                             │ 
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤   
  │ orchestration/transitions.py 的 guard    │ 输出解析 harness（JSON + 关键词兜底）      │ 
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤   
  │ observability/tracer.py                  │ 观测 harness（状态/token/耗时结构化）      │ 
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤ 
  │ observability/tool_observer.py           │ 工具 harness（装饰器埋点）                 │ 
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤ 
  │ tools/instrumented.py（你正打开的文件）  │ 工具 harness（装饰后的 FunctionTool）      │   
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤ 
  │ memory/store.py +                        │ 记忆 harness（写入策略 + 召回 + 注入格式） │   
  │ _retrieve_memory_context                 │                                            │   
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤
  │ task_service.py 的 HITL 分支             │ 人机协作 harness（checkpoint + 暂停 +      │   
  │                                          │ 恢复）                                     │   
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤
  │ models/factory.py                        │ 模型适配 harness（多 provider 切换）       │   
  ├──────────────────────────────────────────┼────────────────────────────────────────────┤   
  │ WebSocket 推送                           │ 输出流 harness（从 run_stream              │
  │                                          │ 分叉到多个消费者）                         │   
  └──────────────────────────────────────────┴────────────────────────────────────────────┘
                                                                                              
  反过来说：这个项目如果把 harness 层全剥掉，就只剩 5 个 AutoGen AssistantAgent + 一个        
  SelectorGroupChat——那是一个 demo，不是产品。
                                                                                              
  ---             
  为什么会有"多 agent 不好做 harness"的错觉？
                                                                                              
  可能来自三个角度的混淆：
                                                                                              
  混淆 1：把"harness"等同于"Claude Code / Cursor 那种单 agent 产品的工程层"

  这是特定产品的 harness，不是 harness 概念本身。那些产品解决的是"一个 agent 怎么用好工具"，多
   agent 系统解决的是"一组 agent 怎么协作"——harness 都要做，只是问题不同。
                                                                                              
  混淆 2：多 agent 框架（AutoGen / CrewAI）看起来"开箱即用"                                   
   
  用 AutoGen 确实能几行代码起一个 demo，感觉不用做                                            
  harness——但那是玩具级。本项目就是证据：AutoGen
  给的只是骨架，要做到生产级（可观测、可控、可介入、可记忆、可持久化）必须自己做大量 harness。
                  
  混淆 3：多 agent 的 harness 有一部分被"编排框架"承担了                                      
   
  RoundRobin / Selector / 消息路由这些在单 agent 里不存在，在多 agent 里由 AutoGen            
  承担。所以直观上感觉 harness 少了——实际上是harness 层更厚了，只是有一层被框架接走了。
                                                                                              
  ---             
  多 agent harness 工程更难、更多、更有价值
                                                                                              
  更难在哪
                                                                                              
  1. 状态空间爆炸：单 agent 只有"当前上下文"一个维度，多 agent                                
  还要管"谁该说话、消息怎么传、何时收束"
  2. 错误传播：一个 agent 输出错了，下游全错；harness 层要做 guard、验证、熔断                
  3. Token 分布不均：某个 agent 可能吃掉 70% 预算，harness 要做分桶观测才能发现               
  4. 一致性：多个 agent 说的内容要互相对得上——harness 要做消息压缩、幻觉检测                  
  5. 可观测性更复杂：单 agent 一条时间线，多 agent 是树/图结构                                
                                                                                              
  更多在哪                                                                                    
                  
  除了单 agent 的所有 harness 工作，还要做：                                                  
  - Agent 选择策略（LLM selector / FSM / handoff）
  - 状态机熔断（max_rounds / max_revisions）                                                  
  - 消息历史裁剪（谁看什么、看多少）        
  - Agent 级 cache（system prompt 可独立缓存）                                                
  - 跨 agent 共享状态（memory、工具状态）                                                     
  - HITL 的阶段插入点                           